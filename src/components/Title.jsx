import React from 'react'

const Title = ({title}) => {
  return (
    <div className=''>
        <h3 className='pt-50 font-medium uppercase text-(--primary-color) border-b pb-3'>{title}</h3>
    </div>
  )
}

export default Title